#!/bin/bash
# ============================================================
# START_BOT_BACKGROUND.command — Double-click to start silently
# ============================================================
# This file opens in Terminal automatically on Mac.
# The bot runs in the BACKGROUND — you can close this window
# after you see "Bot started in background."
# ============================================================
cd "$(dirname "$0")"
bash START_BOT_BACKGROUND.sh
