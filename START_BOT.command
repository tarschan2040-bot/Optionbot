#!/bin/bash
# ============================================================
# START_BOT.command — Double-click this to start the bot
# ============================================================
# This file opens in Terminal automatically on Mac.
# The bot runs in the foreground — close this window to stop.
# ============================================================
cd "$(dirname "$0")"
bash START_BOT.sh
